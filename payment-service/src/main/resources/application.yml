server:
  port: 9093

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/paymentDB
    username: postgres
    password: dream1234
    driverClassName: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
  sql:
    init:
      platform: postgresql
